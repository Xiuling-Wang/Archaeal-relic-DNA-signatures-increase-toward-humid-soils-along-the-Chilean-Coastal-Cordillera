# Session information

Add R and package versions before submission.

Suggested command:

```r
sessionInfo()
```

For reproducible package snapshots, consider adding `renv.lock` after the analysis scripts are finalized.
